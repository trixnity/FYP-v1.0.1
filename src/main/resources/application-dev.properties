spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/fypdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:admin}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

server.port=8080

# Dev-only throwaway. NEVER used in production (prod profile requires JWT_SECRET).
jwt.secret=${JWT_SECRET:dev-only-insecure-secret-not-for-production-use}
jwt.expiration=86400000

stockfish.path=C:\\Users\\Surface Laptop 4\\Downloads\\Stockfish-master\\Stockfish-master\\src\\stockfish.exe
puzzles.import.path=C:\\Users\\Surface Laptop 4\\OneDrive\\Documents\\Sem 7\\FYP1\\db csv untuk retrieval\\lichess_db_puzzle.csv
puzzle.vision.python=python
puzzle.vision.script=tools/vision/chess_vision_to_fen.py
puzzle.vision.model=models/chess-yolo11/best.pt
puzzle.upload.storage-dir=uploads/puzzle-library
puzzle.ai.base-url=http://localhost:8000
puzzle.recognition.storage-dir=uploads/puzzles

stripe.secret-key=${STRIPE_SECRET_KEY:}
stripe.webhook-secret=${STRIPE_WEBHOOK_SECRET:}
stripe.currency=myr
stripe.success-url=http://localhost:8080/api/payments/checkout/success?session_id={CHECKOUT_SESSION_ID}
stripe.cancel-url=http://localhost:8080/api/payments/checkout/cancel

# Receipt issuer details (shown on the PDF receipt). Fill in for a usable receipt.
receipt.issuer.name=${RECEIPT_ISSUER_NAME:EduChess Academy (DEV)}
receipt.issuer.registration-no=${RECEIPT_ISSUER_REG_NO:}
receipt.issuer.address=${RECEIPT_ISSUER_ADDRESS:}
receipt.issuer.email=${RECEIPT_ISSUER_EMAIL:}
receipt.issuer.phone=${RECEIPT_ISSUER_PHONE:}
receipt.currency-prefix=${RECEIPT_CURRENCY_PREFIX:RM}
# Free-text line printed on the receipt (e.g. a tax-relief reference). You are responsible
# for its accuracy - the system does not verify tax deductibility.
receipt.tax-note=${RECEIPT_TAX_NOTE:}

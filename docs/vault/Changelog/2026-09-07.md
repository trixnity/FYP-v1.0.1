---
date: 2026-09-07
type: dev-journal
tags: [changelog, deployment]
---

# 2026-09-07

## Summary
Switched the deployment target from Railway to **Koyeb** (free tier) after the user
hit an unspecified error on Railway.

## Changes
- Deleted `railway.json`.
- `Dockerfile`: JVM flags for a ~512 MB free instance
  (`-XX:MaxRAMPercentage=70 -XX:+UseSerialGC`); comment de-Railway'd.
- `.github/workflows/publish-image.yml` (new): builds the image on GitHub runners and
  pushes to `ghcr.io/trixnity/fyp-v1.0.1:latest` on push to `main` — a build path that
  never runs on a size-limited free instance.
- `DEPLOYMENT.md §0` rewritten for Koyeb:
  - §0.1 Aiven MySQL free plan (Koyeb has no managed MySQL).
  - §0.2 Option A (Koyeb builds the Dockerfile) / Option B (deploy the GHCR image).
  - §0.3 env-var table (dropped Railway `${{MySQL.*}}` refs).
  - §0.4 free-instance caveats (RAM, non-persistent uploads).
  - Renumbered §4-7 → §3-6; §2 generalised.

## Open question
- **The Railway error is still unknown.** If it was a Docker *build* failure
  (`mvnw package` OOM, `apt-get install stockfish`, multi-stage COPY) it will recur on
  Koyeb Option A *and* in the GitHub Actions build. Asked the user to paste the log.

## Verified
- Workflow YAML parses; `DEPLOYMENT.md` has no stale Railway refs.
- Dockerfile change is comment + JVM flags only — no build-logic change.

## Follow-ups
- [ ] Get the Railway error and fix the root cause if it's a build problem.
- [ ] Create Aiven MySQL + Koyeb service; run the workflow once; set image public.
- [ ] `JwtAuthFilter.java` still carries the unrelated import-reorder diff, uncommitted.
- [ ] Remove junk tracked file `tr sk_test srcmainresourcesapplication-dev.properties`.

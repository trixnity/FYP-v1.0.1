---
date: 2026-09-04
type: dev-journal
tags: [changelog, deployment]
---

# 2026-09-04

## Summary
Committed the payments hardening (pushed), then dropped Render entirely.

## Changes
- Pushed `667c87c` (Stripe webhook / idempotency / tax-grade receipts) to `origin/main`.
- Deleted `render.yaml`.
- `DEPLOYMENT.md`: removed the Render section; §2 is now generic "any Docker host",
  title/intro point at Railway only.
- `.dockerignore`: dropped the now-dead `render.yaml` entry.

## Decisions
- Go-live host is **Railway** (Docker build). Confirmed with the user; not revisiting.

## Follow-ups
- [ ] Create the Railway project + MySQL, set env vars, generate domain, redeploy.
- [ ] `JwtAuthFilter.java` still carries the unrelated import-reorder diff, uncommitted.
- [ ] Remove junk tracked file `tr sk_test srcmainresourcesapplication-dev.properties`.

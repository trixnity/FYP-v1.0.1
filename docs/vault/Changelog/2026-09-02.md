---
date: 2026-09-02
type: dev-journal
tags: [changelog]
---

# 2026-09-02

## Summary
Set up an Obsidian vault under `docs/vault/` and wired a Claude Code Stop hook so
future sessions that change code automatically record a dev-journal entry here.

## Changes
- Created `docs/vault/` as an Obsidian vault (`.obsidian/` config, `Changelog/`, `templates/`).
- Added `scripts/vault-journal-hook.cjs` — Node Stop-hook script (no external deps).
- Registered the hook in `.claude/settings.json` under `hooks.Stop`.
- `.gitignore`: kept `.claude/` local but un-ignored `.claude/settings.json` so the
  shared hook travels with the repo; ignored `docs/vault/.journal-state`.

## Files touched
- `docs/vault/**` (new)
- `scripts/vault-journal-hook.cjs` (new)
- `.claude/settings.json` (new)
- `.gitignore`

## Project state snapshot (uncommitted at time of writing)
- `StockfishService.java` — Render/Docker engine configuration in progress (+52 lines).
- `JwtAuthFilter.java` — auth filter adjustments.
- `Dockerfile`, `DEPLOYMENT.md` — deployment tweaks.
- `tools/vision/chess_vision_to_fen.py` — minor changes.
- Last commit: `194fce4 Configure Stockfish for Render`.

## Decisions / notes
- Journal lives in-repo (versioned with the code), not in OneDrive.
- Loop-safe: the hook checks `stop_hook_active` and a `docs/vault/.journal-state`
  signature (HEAD sha + `git status` digest); unchanged state = silent skip.

## Follow-ups
- [ ] Finish and commit the Stockfish-on-Render configuration.
- [ ] Open the vault in Obsidian once to confirm it loads.
- [ ] Commit the vault + hook (`docs/vault/`, `scripts/`, `.claude/settings.json`, `.gitignore`).

## Update 2026-09-03
While this was being set up, a parallel Claude session (`fyp-14`) started an
`appmod/java-upgrade` migration that `git stash --include-untracked`'d the whole
working tree and parked the repo on branch `appmod/java-upgrade-20260902091615`.
The vault files rode safely in that auto-stash. The migration then went idle for
~17h (one uncommitted `pom.xml` edit, no commits), so on 2026-09-03 the work was
restored: `pom.xml` parked in its own stash for `fyp-14`, repo switched back to
`main`, and the vault stash popped. Everything is back and the hook is re-baselined.

---
date: 2026-09-03
type: dev-journal
tags: [changelog, tooling]
---

# 2026-09-03

## Summary
Restored the vault work that a parallel java-upgrade migration had stashed, then
installed the `taste-skill` design-skill pack (13 skills) into `.claude/skills/`.

## Changes
- **Vault restore:** parked session `fyp-14`'s uncommitted `pom.xml` edit in its own
  git stash, switched repo back to `main`, popped the auto-stash. Vault + hook back
  in place, hook re-baselined. See [[2026-09-02]] for the full setup.
- **Skills install:** ran `npx skills add https://github.com/Leonxlnx/taste-skill
  --skill '*' --agent claude-code -y --copy`. Installed 13 skills (copied, not
  symlinked) under `.claude/skills/`:
  - design-taste-frontend, design-taste-frontend-v1, gpt-taste, minimalist-ui,
    industrial-brutalist-ui, high-end-visual-design, redesign-existing-projects,
    image-to-code, full-output-enforcement, brandkit, stitch-design-taste,
    imagegen-frontend-web, imagegen-frontend-mobile
  - `skills-lock.json` written at repo root (restore with `npx skills experimental_install`).

## Files touched
- `.claude/skills/**` (new, gitignored via `.claude/*`)
- `skills-lock.json` (new, repo root — trackable)
- `docs/vault/Changelog/2026-09-03.md`, `_index.md`

## Decisions / notes
- Skills are project-scoped and local-only (`.claude/` is gitignored here). The
  lockfile is the shareable record.
- All 13 SKILL.md files validated: each has `name` + `description` frontmatter.
- These skills are heavy on frontend/landing-page design; the EduChess UI is
  server-rendered static assets, so relevance is situational.
- The parallel java-upgrade migration on branch `appmod/java-upgrade-20260902091615`
  is still unmerged and its `pom.xml` WIP sits in `git stash`.

## Follow-ups
- [x] Committed + pushed the vault + hook + `skills-lock.json` (commit `e7639e8` on `main`).
- [ ] New Claude Code session (or reload) needed before the installed skills are active.
- [ ] Resolve or abandon the `appmod/java-upgrade` branch + its stashed `pom.xml`.

---

## Railway deploy migration (Render was failing)

### Why Render failed
- `render.yaml` uses `env: java` (native buildpack) so the `Dockerfile` was ignored →
  Stockfish never installed, and `./mvnw clean package` OOMs on the 512 MB free instance.

### Changes (uncommitted, pending user go-ahead)
- `railway.json` (new): `builder: DOCKERFILE`, healthcheck `GET /`, restart ON_FAILURE.
- `Dockerfile`: `--no-install-recommends` + build-time `test -x /usr/games/stockfish`,
  `-XX:MaxRAMPercentage=75.0`, default `SPRING_PROFILES_ACTIVE=prod`, honour injected `PORT`.
- `.dockerignore`: trimmed build context to `.mvn / mvnw / pom.xml / src`.
- `DEPLOYMENT.md`: new "§0 Deploy to Railway (recommended)" — project setup, add-MySQL,
  `${{MySQL.*}}` reference vars, domain/health notes, optional volume for uploads.
  Render section marked as the fallback (switch it to `env: docker` if kept).

### Verified
- `./mvnw -q -B -DskipTests compile` → exit 0 (uncommitted StockfishService changes build).
- Could not run `docker build` — no Docker CLI on this machine; Railway will build.

### Notes / found while reviewing
- `tools/vision/chess_vision_to_fen.py` has a **stray `x` statement** (after `crop_board`)
  — accidental keystroke, will `NameError` on import. Not in the container, so it does not
  block the deploy, but it should be removed.
- `JwtAuthFilter.java` uncommitted diff is only IDE import reordering + trailing blank
  lines — no logic change.
- Plan offered to the user: commit `railway.json`, `Dockerfile`, `.dockerignore`,
  `DEPLOYMENT.md`, `StockfishService.java` as one "deploy to Railway" commit; leave the
  two unrelated edits out; optionally fix the stray `x`. Awaiting confirmation.

### Follow-ups
- [ ] User confirms → commit + push the Railway deploy config.
- [ ] Create the Railway project + MySQL service, set env vars, generate domain.
- [ ] Fix the stray `x` in `chess_vision_to_fen.py`.
- [ ] Decide whether to delete `render.yaml`.
